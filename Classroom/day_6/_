#include<stdio.h>
#include<signal.h>
#include<unistd.h>

void handler(int sig_no)
{
	printf("We are handling ctrl+c\n");
}

int main()
{
	int i=0;
	signal(SIGINT,  handler);
	while(1)
	{
		printf("%d\n", i++);
		sleep(1);
	}
	return 0;
}
